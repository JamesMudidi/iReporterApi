language: python

python:
  - "3.6"

install: 
  - pip install -r requirements.txt
  - pip install coveralls

script:
  - pytest
  - coveralls 
  - coverage report

after_success:
  - coveralls
  - coverage report
