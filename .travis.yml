language: python
  
python:
  - "3.6.5"
install:
  - pip install -r requirements.txt

script:
  - pytest --cov=run.py
  - coverage report
  - coveralls 

after_success:
  - coveralls
  - coverage report
