language: python

python:
  - "3.6.5"
  
install:
  - pip install -r requirements.txt
  - pip install coveralls

script:
  # - pytest
  - coverage
  - coveralls 

after_success:
  - coverage
  - coveralls