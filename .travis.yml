language: python
  
python:
  - "3.6.5"
install:
  - pip install -r requirements.txt

script:
  - pytest --cov
  - coverage run run.py
  - coveralls 

after_success:
  - coveralls
  - coverage report
