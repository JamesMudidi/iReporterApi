language: python
  
python:
  - "3.6.5"
install:
  - pip install -r requirements.txt

before_success:
  - coverage run run.py

script:
  - pytest
  - coverage report
  - coveralls 

after_success:
  - coveralls
  - coverage report
