language: python

python:
  - "3.6.5"
install:
  - pip install -r requirements.txt

before_success:
  - coveralls
  - coverage
  - coverage3 report

script:
  - pytest --cov
  - coverage
  - coverage3 report
  - coveralls 

after_success:
  - coveralls
  - coverage
  - coverage3 report
