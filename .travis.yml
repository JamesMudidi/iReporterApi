language: python
  
python:
  - "3.6.5"
install:
  - pip install -r requirements.txt

# before_success:
#   - coveralls
#   - coverage report

script:
  - pytest --cov
  - coverage report
  - coveralls 

after_success:
  - coveralls
  - coverage report
